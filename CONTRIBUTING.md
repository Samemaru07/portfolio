# Contributing

## 記法ルール

- 箇条書きは体言止め（「〜を行う」ではなく「〜の実行」）
- 句読点は全角（。、）で統一
- 見出しは`##`のみ使用し、`###`以下は使わない（章を増やしすぎない）
- コード・コマンド・ファイル名はバッククォートで囲む
- 例外や曖昧な言い回し（「基本的に」「場合によっては」）は避け、断定形で書く
- 1項目1ルール。複数条件を1行に詰め込まない

## Gitの各機能の目的明確化

- **ブランチ**: 開発作業の全体的な目的・タスク分類を示す。
- **コミット**: そのタスクの中で、コード変更の物理的なアクション・作業単位

## 共通ルール

- 句読点を用いる。
- カッコは半角カッコを用い、`(` の前と、 `)` の後ろには半角スペースを空ける。
- 文末は「○○を追加した。」や「○○を実装した。」でなく、「○○を追加。」や「○○を実装。」のようにする。

## ブランチ運用

- **GitHub Flow**をベースとしたフローで行う。
- ブランチの命名規則は以下。
    - **mainブランチ**: 常にリリース可能な最新バージョンを保持する。
        - 直接コミットしない。
    - **featureブランチ**: 新しい機能を開発するためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `feature/機能名` (例: `feature/add-get-user-api`)
    - **removeブランチ**: 機能を削除するためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `remove/削除する機能名` (例: `remove/user-counter`)
    - **fixブランチ**: バグ・不具合修正ブランチ
        - mainブランチから派生する。
        - 命名規則: `fix/バグ修正名` (例: `fix/login-error`)
    - **docsブランチ**: README, 仕様書, APIリファレンス, ライセンス等の作成・修正のみを行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `docs/対象ドキュメント名` (例: `docs/readme`)
    - **refactorブランチ**: コードのリファクタリングを行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `refactor/リファクタリング対象名` (例: `refactor/component`)
    - **choreブランチ**: リポジトリの整理 (不要なファイル削除・ファイルの移動等) を行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `chore/整理する対象名` (例: `chore/file-delete`)
    - **perfブランチ**: パフォーマンス改善 (処理速度の高速化, クエリ最適化, メモリ使用量の削減など) を行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `perf/最適化対象名` (例: `perf/optimize-sql-query`)
    - **testブランチ**: テストコードの追加・修正・拡充を行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `test/テスト対象名` (例: `test/user-auth-api`, `test/add-payment-unit-test`)
    - **ciブランチ**: CI/CDパイプラインやビルド・デプロイワークフローの実装・調整を行うためのブランチ。
        - mainブランチから派生する。
        - 命名規則: `ci/変更対象名` (例: `ci/github-actions-workflow`, `ci/fix-docker-build`)
    - **depsブランチ**: 依存関係の更新 (ライブラリ・フレームワーク等のバージョンアップ・脆弱性対応) を行うブランチ。
        - mainブランチから派生する。
        - 命名規則: `deps/更新するパッケージ名` (例: `deps/bump-react-to-19`)
- mainへのPRによるマージは、基本的にマージコミットを用いて、コミットメッセージにブランチ名を残す。

## コミット規約

- コミットは、1つの作業単位ごとに行う。
- タイトルは、句点は用いない。
- ボディは、句点を用いてもよい。
- 形式は `type(scope): ○○ため、△△を変更` の形式 (理由・原因 + 行為) 。
    - `type` には、以下のいずれかが入る。
        - `add`: 新規ファイル・クラス・メソッド・依存関係の追加。
        - `update`: 既存のコードや設定の仕様変更・挙動の更新。
        - `fix`: typo (誤字) ・構文ミス・小さなバグの修正。
        - `remove`: 不要なコード・ファイル・依存パッケージの削除。
        - `refactor`: 外部の挙動を変えない内部構造の整理・命名変更。
        - `test`: 単体テストやモックの追加・更新。
    - `scope` には、変更が影響するモジュールやディレクトリ名を指定する (小文字英単語)。
        - `api`: APIエンドポイント・ルーティング関連。
        - `ui`: 画面コンポーネント・スタイル関連。
        - `model`: データベース・スキーマ・型定義関連。
        - `auth`: 認証・認可関連。
        - `config`: 各種設定ファイル・環境変数関連。
        - `deps`: 依存パッケージの追加・更新・削除。
        - `ci`: CI/CD設定・ワークフロー関連。
        - `docs`: ドキュメント・README関連。
        - 複数領域にまたがる全体変更の場合は、`global` を指定するか、scopeの記述を省略する。
    - `type` と `scope` が同じになる場合、または対象が自明な場合は、`scope` を省略して `type: ○○ため、△△を変更` とする。

## PRフォーマット

- 形式は以下。
- タイトル: `type(scope): ○○ため、△△を変更` の形式 (理由・原因 + 行為) 。
    - `type` は、ブランチ名のprefixと揃える。
    - `type` と `scope` が同じになる場合、`scope` を省略する。
- ボディ: 以下のように基本箇条書きを用いる。
    - 関連Issue, 動作確認の小項目, 備考はプロジェクト・PRによって無くてもよい。

```
## 関連Issue
- close #Issue番号

## 概要・目的
- なぜこの変更を行うのか（背景・課題・目的）を簡潔に記述。

## 変更内容
- `変更を行ったファイルやディレクトリ`
    - 具体的に行った修正・追加内容
    - 具体的に行った修正・追加内容

## 影響範囲・破壊的変更
- 既存への影響: なし / あり（具体的に記載）
- 設定等の変更: 環境変数の追加やマイグレーションの要否

## 動作確認
- [ ] テストの実行・通過
- [ ] ローカルでの画面・APIの動作確認
- [ ] ビルド確認

## 備考
- 実装上の懸念点、保留した事項などがあれば記述（なければ空欄で可）。
```

## PRによるマージについて

- 基本的に、マージコミットを作成する。
- 形式は以下。
    - タイトル: `Merge pull request #<PR番号> from <ユーザー名>/<ブランチ名>` (例: `Merge pull request #12 from owner/feature/add-get-user-api`)
    - ボディ: PRタイトル

## タグ・バージョン管理

- セマンティックバージョニング (`v<メジャー>.<マイナー>.<パッチ>`) (例: `v1.1.0`) を用いる。
- 採番タイミングはmainにマージした後だけ。

## コードスタイル

- インデントは4文字分。
- 文字列は基本ダブルクォーテーションで囲む。
- コメントは日本語。ロジックの冒頭や、かたまりの最初に、「理由」が分かるように書く。
